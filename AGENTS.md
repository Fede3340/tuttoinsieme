## Repository rules

- Non committare segreti o credenziali: usa solo file di esempio per le variabili d’ambiente.
- Quando una modifica coinvolge flussi completi, aggiorna sia interfaccia sia server.
- Il portafoglio deve essere idempotente (nessuna duplicazione dei movimenti).
- Il saldo del portafoglio deve essere calcolato esclusivamente dai movimenti.
